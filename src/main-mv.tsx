import React from 'react'
import ReactDOM from 'react-dom/client'
import { MVPoseAnnotationTool } from './components/MVApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <MVPoseAnnotationTool />
  </React.StrictMode>,
)
